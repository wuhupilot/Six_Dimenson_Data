# DataSolverLib 静态库使用说明

## 一、什么是静态库？

静态库（.lib文件）是一个包含编译好的代码的文件，其他程序在编译时会将库的代码链接到最终的可执行文件中。

**注意**：这是**静态库（.lib）**，不是动态库（.dll）。

## 二、如何构建静态库

### 步骤1：打开项目

1. 在 Windows 文件资源管理器中，导航到项目目录
2. 双击 `Data_Solve.sln` 用 Visual Studio 2022 打开

### 步骤2：选择构建目标

1. 在 Visual Studio 顶部工具栏，确保选择：
   - **配置**：`Release`（或 `Debug`，根据需要）
   - **平台**：`Win32`（32位）

### 步骤3：构建静态库

**方法A：在 Visual Studio 中构建**
1. 在"解决方案资源管理器"中，右键点击 `DataSolverLib` 项目
2. 选择"生成"（Build）
3. 等待构建完成

**方法B：使用命令行构建**
1. 打开"开发人员命令提示符 for VS 2022"
2. 切换到项目目录：
   ```cmd
   cd C:\Users\wrf\Desktop\Data\Data_Solve
   ```
3. 执行构建命令：
   ```cmd
   msbuild Data_Solve.sln /t:DataSolverLib /p:Configuration=Release /p:Platform=Win32
   ```

### 步骤4：找到生成的库文件

构建成功后，库文件位于：
- **Release 版本**：`Win32\Release\DataSolverLib.lib`
- **Debug 版本**：`Win32\Debug\DataSolverLib.lib`

## 三、如何使用静态库

### 方式1：简单调用（最简单的方式）

如果您只需要处理CSV文件，创建一个简单的C程序即可：

**文件：test_lib.c**
```c
#include "DataSolverLib.h"
#include <stdio.h>

int main() {
    char errorMsg[512];

    // 处理CSV文件
    int result = ProcessDataFile(
        "C:\\input.csv",      // 输入文件路径
        "C:\\output.csv",     // 输出文件路径
        errorMsg,             // 错误信息
        512                   // 错误信息缓冲区大小
    );

    if (result == 0) {
        printf("成功！结果已保存到 output.csv\n");
    } else {
        printf("失败：%s\n", errorMsg);
    }

    return 0;
}
```

### 方式2：在现有项目中使用

#### 1. 复制必要文件到您的项目

将以下文件复制到您的项目目录：
```
您的项目目录/
├── DataSolverLib.h          # 头文件
├── DataSolverLib.lib        # 静态库文件
└── 您的代码.cpp
```

#### 2. 在 Visual Studio 中配置项目

**步骤A：添加头文件目录**
1. 右键点击项目 → 属性
2. C/C++ → 常规 → 附加包含目录
3. 添加 `DataSolverLib.h` 所在目录

**步骤B：添加库文件**
1. 链接器 → 输入 → 附加依赖项
2. 添加：`DataSolverLib.lib`

**步骤C：添加库目录**
1. 链接器 → 常规 → 附加库目录
2. 添加 `DataSolverLib.lib` 所在目录

**步骤D：添加 Qt 依赖**

因为静态库使用了Qt，您的项目也需要链接Qt库：
1. 链接器 → 输入 → 附加依赖项，添加：
   ```
   Qt5Core.lib
   ```
2. 链接器 → 常规 → 附加库目录，添加：
   ```
   C:\Qt\5.15.2\msvc2019\lib
   ```
   （根据您的Qt安装路径调整）

3. C/C++ → 常规 → 附加包含目录，添加：
   ```
   C:\Qt\5.15.2\msvc2019\include
   C:\Qt\5.15.2\msvc2019\include\QtCore
   ```

## 四、API 函数说明

### 1. ProcessDataFile - 处理所有通道

```c
int ProcessDataFile(
    const char* inputCsvPath,      // 输入CSV文件路径
    const char* outputCsvPath,     // 输出CSV文件路径（NULL=自动生成）
    char* errorMessage,            // 错误信息缓冲区
    int errorMessageSize           // 缓冲区大小
);
```

**功能**：
- 读取输入CSV文件
- 自动处理所有6个通道（Fx, Fy, Fz, Mx, My, Mz）
- 导出完整的分析报告到CSV文件

**返回值**：
- `0` - 成功
- `1` - 输入文件不存在
- `2` - 文件格式无效
- `3` - 数据处理失败
- `4` - 导出失败
- `5` - 无效参数

**示例**：
```c
char error[512];
int status = ProcessDataFile("input.csv", "output.csv", error, 512);
if (status == 0) {
    printf("成功！\n");
} else {
    printf("错误：%s\n", error);
}
```

### 2. ProcessChannelDataFile - 处理单个通道

```c
int ProcessChannelDataFile(
    const char* inputCsvPath,      // 输入CSV文件路径
    const char* channelName,       // 通道名称："Fx", "Fy", "Fz", "Mx", "My", "Mz"
    const char* outputCsvPath,     // 输出CSV文件路径（NULL=自动生成）
    char* errorMessage,            // 错误信息缓冲区
    int errorMessageSize           // 缓冲区大小
);
```

**功能**：只处理指定的单个通道

**示例**：
```c
// 只处理 Fx 通道
char error[512];
int status = ProcessChannelDataFile("input.csv", "Fx", "output_fx.csv", error, 512);
```

### 3. ProcessDataFileWithPreprocessing - 数据预处理+处理（v2.1新增）

```c
int ProcessDataFileWithPreprocessing(
    const char* inputCsvPath,       // 输入CSV文件路径
    const char* outputCsvPath,      // 输出CSV文件路径
    int averageGroupSize,           // 平均分组大小（0=不平均）
    double zeroThreshold,           // 零点阈值（默认0.1）
    int enableZeroCalibration,      // 是否启用置零校准（1=启用，0=不启用）
    char* errorMessage,             // 错误信息缓冲区
    int errorMessageSize            // 缓冲区大小
);
```

**功能**：
- 自动执行完整的数据预处理流程：数据平均 → 置零校准 → 数据处理 → 导出
- 适用于需要对原始数据进行预处理的场景

**参数说明**：
- `averageGroupSize`: 每几行求平均（例如：5表示每5行求平均），设为0则跳过平均步骤
- `zeroThreshold`: 判断是否为零点的阈值（绝对值小于该值视为零点）
- `enableZeroCalibration`: 1=启用置零校准，0=不启用

**示例**：
```c
char error[512];
// 每5行平均，阈值0.1，启用置零校准
int status = ProcessDataFileWithPreprocessing(
    "raw_data.csv",      // 输入文件
    "processed.csv",     // 输出文件
    5,                   // 每5行平均
    0.1,                 // 零点阈值
    1,                   // 启用置零校准
    error,
    512
);

if (status == 0) {
    printf("预处理+处理成功！\n");
    printf("处理日志: %s\n", error);  // error包含处理统计信息
}
```

### 4. SetChannelRanges - 设置量程

```c
void SetChannelRanges(
    double fxRange,  // Fx通道量程
    double fyRange,  // Fy通道量程
    double fzRange,  // Fz通道量程
    double mxRange,  // Mx通道量程
    double myRange,  // My通道量程
    double mzRange   // Mz通道量程
);
```

**功能**：设置各通道的量程（用于误差计算）

**默认值**：Fx=1000, Fy=1000, Fz=1500, Mx=50, My=50, Mz=50

**示例**：
```c
// 在处理数据前设置量程
SetChannelRanges(1000.0, 1000.0, 1500.0, 50.0, 50.0, 50.0);
ProcessDataFile("input.csv", "output.csv", error, 512);
```

### 5. GetLibraryVersion - 获取版本

```c
const char* GetLibraryVersion();
```

**示例**：
```c
printf("库版本: %s\n", GetLibraryVersion());
```

## 五、完整示例代码

```c
#include "DataSolverLib.h"
#include <stdio.h>

int main() {
    printf("DataSolverLib 版本: %s\n\n", GetLibraryVersion());

    char errorMsg[512];

    // 1. 设置量程（可选）
    SetChannelRanges(1000.0, 1000.0, 1500.0, 50.0, 50.0, 50.0);
    printf("已设置量程\n");

    // 2. 处理数据文件
    printf("正在处理数据...\n");
    int status = ProcessDataFile(
        "test_data.csv",       // 输入文件
        "result.csv",          // 输出文件
        errorMsg,
        sizeof(errorMsg)
    );

    // 3. 检查结果
    if (status == 0) {
        printf("✓ 处理成功！\n");
        printf("结果已保存到: result.csv\n");
    } else {
        printf("✗ 处理失败！\n");
        printf("错误代码: %d\n", status);
        printf("错误信息: %s\n", errorMsg);
    }

    return 0;
}
```

## 六、输入文件格式要求

您的CSV文件必须包含以下列（列的顺序不重要）：

### 必需的列：
- `Fx`, `Fy`, `Fz`, `Mx`, `My`, `Mz` - 设定值（标准力/力矩）
- `UFx`, `UFy`, `UFz`, `UMx`, `UMy`, `UMz` - 测量值（传感器输出）

### 示例CSV文件：
```csv
Fx,Fy,Fz,Mx,My,Mz,UFx,UFy,UFz,UMx,UMy,UMz
0,0,0,0,0,0,0.001,0.002,0.003,0.001,0.002,0.001
100,0,0,0,0,0,0.105,0.003,0.004,0.002,0.003,0.002
200,0,0,0,0,0,0.210,0.004,0.005,0.003,0.004,0.003
```

## 七、输出文件内容

输出的CSV文件包含：

1. **性能指标评价表**
   - 重复性R（%FS）
   - 迟滞性H（%FS）
   - 线性度L（%FS）
   - I类误差（%FS）
   - II类误差（%FS）

2. **各通道详细数据**（Fx, Fy, Fz, Mx, My, Mz）
   - 重复性误差数据
   - 线性度数据

3. **解耦矩阵**
   - 第一矩阵（6×6）
   - 第二矩阵（6×6）
   - 第三矩阵（6×6）

4. **误差分析**
   - 解耦后数据
   - 原始误差
   - 归一化平方误差
   - 通道统计值

## 八、运行时依赖

程序运行时需要以下DLL文件（需要在可执行文件同目录，或在系统PATH中）：

- `Qt5Core.dll`

**获取方式**：
从 Qt 安装目录复制：`C:\Qt\5.15.2\msvc2019\bin\Qt5Core.dll`

## 九、常见问题

### Q1：链接时出现 "无法解析的外部符号" 错误
**A**：确保添加了 `Qt5Core.lib` 到链接器输入。

### Q2：运行时提示缺少 Qt5Core.dll
**A**：将 `Qt5Core.dll` 复制到可执行文件同目录。

### Q3：如何打包给别人使用？
**A**：提供以下文件：
- `DataSolverLib.h` - 头文件
- `DataSolverLib.lib` - 静态库
- `Qt5Core.lib` - Qt库文件
- 使用说明文档

### Q4：支持Excel文件吗？
**A**：输入文件必须是CSV格式。如果有Excel文件，请先在Excel中另存为CSV。

## 十、快速开始检查清单

- [ ] 构建了 `DataSolverLib.lib`
- [ ] 准备了输入CSV文件
- [ ] 创建了调用代码
- [ ] 添加了 `DataSolverLib.h` 到项目
- [ ] 链接了 `DataSolverLib.lib`
- [ ] 链接了 `Qt5Core.lib`
- [ ] 配置了Qt的包含目录和库目录
- [ ] 复制了 `Qt5Core.dll` 到可执行文件目录
- [ ] 编译运行

## 十一、联系支持

如有问题，请查看 `example_lib_usage.cpp` 中的完整示例代码。
