# 数据处理工具 - 开发记录

## 项目概述
这是一个基于Qt 5.15.2开发的力传感器数据处理工具，用于分析CSV/Excel数据文件中的力学测试数据，计算重复性误差和线性度分析。

## 开发历程

### 初始版本
- 基础的Qt桌面应用程序
- 支持CSV和Excel文件加载
- 基本的数据显示功能

### 核心功能开发

#### 1. 重复性误差分析
- **0点数据处理**：4个连续的0值，分组为1,2,3（上升沿）和2,3,4（下降沿）
- **邻近值判断**：0点根据前后邻近的非零值判断属于正值循环还是负值循环
- **边沿类型识别**：上升沿、下降沿、平稳等类型
- **统计计算**：
  - 平均值计算（4位小数精度）
  - 标准差计算：使用样本标准差公式 `SQRT(SUMSQ(values-mean)/(n-1))`
  - 标准偏差计算：`SQRT(SUMSQ(标准差值)/COUNT(标准差个数))`

#### 2. 记录量计算系统
实现了三个层级的记录量：

**第一个记录量**：
```
记录量 = 2 * MAX(正量程标准偏差, 反量程标准偏差) / 210
```

**Last记录量**：
```
Last记录量 = 2 * 记录量 / 210
```

**第三记录量**：
```
第三记录量 = MAX(|所有正行程平均值-反行程平均值|) / 210
```

#### 3. 数据表格结构

**重复性误差表格列**：
1. 循环类型（正值循环/负值循环）
2. Fx设定值
3. 上升沿平均值
4. 上升沿标准差
5. 上升沿数值列表
6. 下降沿平均值
7. 下降沿标准差
8. 下降沿数值列表
9. |正行程平均值-反行程平均值|（绝对值）
10. 正循环标准偏差
11. 负循环标准偏差

**线性度Fx表格列**：
1. Fx设定值
2. UFx平均值
3. 理论值
4. 误差
5. 线性度误差%

#### 4. 用户界面设计

**标签页结构**：
1. **原始数据** - 显示加载的CSV/Excel数据
2. **重复性误差** - 显示重复性分析结果和记录量计算
3. **线性度Fx** - 显示线性度分析结果
4. **统计信息** - 显示详细的计算过程和公式说明

### 技术要点

#### 数据处理算法
```cpp
// 0点分组逻辑
if (points.size() >= 4) {
    // 1,2,3号0点为上升沿
    for (int i = 0; i < 3; ++i) {
        risingValues.append(points[i].ufxValue);
    }
    // 2,3,4号0点为下降沿
    for (int i = 1; i < 4; ++i) {
        fallingValues.append(points[i].ufxValue);
    }
}
```

#### 标准差计算
```cpp
auto calculateStandardDeviation = [](const QVector<double>& values, double mean) -> double {
    if (values.size() <= 1) return 0.0;

    double sumSquares = 0.0;
    for (double val : values) {
        double diff = val - mean;
        sumSquares += diff * diff;
    }

    return sqrt(sumSquares / (values.size() - 1));  // 样本标准差
};
```

#### 绝对值差值计算
```cpp
// 计算正行程平均值-反行程平均值（取绝对值）
if (!risingValues.isEmpty() && !fallingValues.isEmpty()) {
    double difference = qAbs(risingAvg - fallingAvg);
    avgDifference = QString::number(difference, 'f', 4);
    allAvgDifferences.append(difference);
}
```

### 关键修复

#### 1. 编译错误修复
- **问题**：UI文件更新后，`tableLinearityData`成员未找到
- **原因**：Qt的UIC编译器需要重新生成`ui_Data_Solve.h`文件
- **解决**：重新编译项目，Qt自动生成包含新UI元素的头文件

#### 2. 变量重定义修复
- **问题**：C2374错误，`fallingValuesList`重复声明
- **原因**：代码重构时产生重复的变量声明
- **解决**：删除重复的代码块，保持唯一声明

#### 3. 绝对值处理
- **需求**：正行程平均值-反行程平均值需要取绝对值
- **实现**：使用`qAbs()`函数确保所有差值为正数
- **影响**：第三记录量计算基于绝对值差值的最大值

### 项目文件结构
```
Data_Solve/
├── Data_Solve.sln          # Visual Studio解决方案文件
├── Data_Solve.vcxproj      # 项目配置文件
├── main.cpp                # 程序入口点
├── Data_Solve.h            # 主窗口类声明
├── Data_Solve.cpp          # 主窗口类实现
├── Data_Solve.ui           # Qt Designer界面文件
├── Data_Solve.qrc          # Qt资源文件
├── CLAUDE.md              # 构建指令和项目说明
└── Debug/                  # 调试输出目录
    └── qt/uic/
        └── ui_Data_Solve.h # 自动生成的UI头文件
```

### 构建配置
- **开发环境**：Visual Studio 2022 (v143工具集)
- **Qt版本**：5.15.2_msvc2019
- **目标平台**：Windows (Win32)
- **字符集**：Unicode
- **依赖项**：Qt Core, GUI, Widgets

### 构建命令
```bash
# Debug版本
msbuild Data_Solve.sln /p:Configuration=Debug /p:Platform=Win32

# Release版本
msbuild Data_Solve.sln /p:Configuration=Release /p:Platform=Win32
```

### 运行命令
```bash
# Debug版本
Debug\Data_Solve.exe

# Release版本
Release\Data_Solve.exe
```

## 功能特点

### 1. 数据加载
- 支持CSV和Excel文件格式
- 自动UTF-8编码处理
- 使用Python pandas处理Excel文件
- 容错的CSV解析（支持引号和逗号转义）

### 2. 数据处理
- 智能的纯Fx测试数据识别
- 基于邻近值的0点分类
- 重叠分组的0点处理
- 边沿类型自动识别

### 3. 统计分析
- 多层级的标准差和标准偏差计算
- 三个记录量指标
- 线性度误差分析
- 详细的计算过程展示

### 4. 用户体验
- 直观的标签页界面
- 实时状态更新
- 详细的计算过程显示
- 支持数据导出（CSV/Excel）

## 测试数据要求

### 输入数据格式
CSV/Excel文件应包含以下列：
- **Fx**：X轴力设定值
- **UFx**：X轴力测量值
- **Fy, Fz, Mx, My, Mz**：其他力和力矩分量（用于判断纯Fx测试）

### 数据特点
- 支持正负值测试循环
- 自动识别0点数据
- 处理重复测量数据
- 适用于力传感器标定测试

## 输出结果

### 重复性误差分析
- 每个设定值的统计数据
- 上升沿和下降沿分析
- 正负循环标准偏差
- 三个记录量指标

### 线性度分析
- 理论值与测量值对比
- 线性度误差百分比
- 适用于传感器精度评估

## 开发团队
- **主要开发**：Claude Code (claude.ai/code)
- **技术栈**：C++, Qt 5.15.2, Visual Studio 2022
- **开发时间**：2024年9月
- **版本**：v1.0

---

*此文档记录了数据处理工具的完整开发过程，包括核心算法、用户界面设计、技术实现和使用说明。*