# Qt 数据处理工具项目文档

## 项目概述

这是一个用于处理 Last_Data.xlsx 文件的 Qt C++ 桌面应用程序，主要功能是分析 Fx 力传感器的循环测试数据。

## 核心需求

### 测试流程
1. 按顺序测试不同分量：Fx → Fy → Fz → Mx → My → Mz，然后循环
2. 每个分量都有正值和负值两种循环模式
3. Fx 循环模式：0 → 60 → 110 → 160 → 210 → 160 → 110 → 60 → 0

### 数据处理要求
1. **识别纯 Fx 测试**：只有 Fx 有数据，其他分量(Fy,Fz,Mx,My,Mz)接近0
2. **上升沿/下降沿检测**：通过比较前后值判断数据趋势
3. **0点特殊处理**：
   - 正值循环：4个0点，前3个算上升沿，后3个算下降沿
   - 负值循环：4个0点，前3个算上升沿，后3个算下降沿
4. **计算平均值**：每个测试点收集3次测量，计算平均值

## 项目结构

```
Data_Solve/
├── Data_Solve.sln          # Visual Studio 解决方案
├── Data_Solve.vcxproj      # 项目配置文件
├── main.cpp                # 程序入口点
├── Data_Solve.h            # 主窗口头文件
├── Data_Solve.cpp          # 主要处理逻辑
├── Data_Solve.ui           # Qt Designer 界面文件
├── Data_Solve.qrc          # Qt 资源文件
├── CLAUDE.md               # Claude Code 指导文件
└── Last_Data.xlsx          # 待处理的数据文件
```

## 开发环境

- **Qt 版本**: 5.15.2_msvc2019
- **编译器**: Visual Studio 2022 (v143 toolset)
- **平台**: Windows (Win32)
- **编译命令**: `msbuild Data_Solve.sln /p:Configuration=Debug /p:Platform=Win32`

## 关键功能实现

### 1. 数据加载 (loadCSVFile)
- 支持 CSV 和 Excel 格式
- Excel 通过 Python pandas 转换为 CSV
- 自动识别列标题

### 2. 数据处理核心逻辑 (processData)

#### 步骤1：识别纯 Fx 测试
```cpp
// 检查其他分量是否接近0
bool isPureFxTest = true;
for (int colIndex : otherColumns) {
    double value = row[colIndex].toDouble(&ok);
    if (ok && qAbs(value) > 0.1) {
        isPureFxTest = false;
        break;
    }
}
```

#### 步骤2：边沿检测
```cpp
// 比较前后值判断趋势
if (prevFx < fxValue && fxValue < nextFx) {
    edgeType = "上升";
} else if (prevFx > fxValue && fxValue > nextFx) {
    edgeType = "下降";
}
```

#### 步骤3：0点特殊处理
```cpp
// 正值循环和负值循环分别处理
if (qAbs(fxValue) < 0.1) {
    if (points.size() >= 4) {
        // 上升沿：第1、2、3个0点
        risingValues.append(points[0].ufxValue);
        risingValues.append(points[1].ufxValue);
        risingValues.append(points[2].ufxValue);

        // 下降沿：第2、3、4个0点
        fallingValues.append(points[1].ufxValue);
        fallingValues.append(points[2].ufxValue);
        fallingValues.append(points[3].ufxValue);
    }
}
```

#### 步骤4：0点分配逻辑
```cpp
// 8个0点：前4个给正值循环，后4个给负值循环
if (zeroPoints.size() >= 8) {
    for (int i = 0; i < 4; ++i) {
        positiveFxGroups[0.0].append(zeroPoints[i]);
    }
    for (int i = 4; i < 8; ++i) {
        negativeFxGroups[0.0].append(zeroPoints[i]);
    }
}
```

### 3. 界面设计
- 三个标签页：原始数据、处理后数据、统计信息
- 控制按钮：加载数据、处理数据、导出结果、清空数据
- 状态栏显示处理进度

## 输出结果格式

| 循环类型 | Fx设定值 | 上升沿平均值 | 上升沿次数 | 下降沿平均值 | 下降沿次数 | 总测量次数 |
|---------|---------|-------------|----------|-------------|----------|----------|
| 正值循环 | 0.0     | 1.234       | 3        | 1.189       | 3        | 4        |
| 正值循环 | 60.0    | 15.678      | 3        | 15.234      | 3        | 6        |
| 负值循环 | 0.0     | 0.987       | 3        | 0.945       | 3        | 4        |
| 负值循环 | -60.0   | -15.432     | 3        | -15.678     | 3        | 6        |

## 重要技术细节

### 编码问题解决
```cpp
#pragma execution_character_set("utf-8")  // 解决中文字符编译错误
```

### Python集成
```cpp
// Excel转CSV使用Python pandas
QString pythonScript = QString(
    "import pandas as pd\n"
    "df = pd.read_excel('%1', sheet_name=0)\n"
    "df.to_csv('%2', index=False, encoding='utf-8')\n"
).arg(excelPath).arg(tempPath);
```

## 常见问题和解决方案

1. **编译错误 C2001**: 中文字符编码问题
   - 解决方案：添加 `#pragma execution_character_set("utf-8")`

2. **0点计算次数过多**: 边沿检测逻辑问题
   - 解决方案：0点不使用边沿检测，按位置分组

3. **Excel读取失败**: Python环境问题
   - 解决方案：手动转换为CSV格式或安装pandas库

## 下次开发提示

如果需要继续开发，请提供：
1. 当前遇到的具体问题
2. 相关的代码片段
3. 数据文件的结构说明
4. 期望的处理结果

这样我就能快速理解项目状况并继续协助开发。

## 项目状态

- ✅ 基础界面设计完成
- ✅ 数据加载功能完成
- ✅ Fx数据处理逻辑完成
- ✅ 0点特殊处理完成
- ⏳ 其他分量(Fy,Fz,Mx,My,Mz)处理待开发
- ⏳ 数据导出功能待完善

最后更新：2025-01-20